"""automation-local: a tiny CLI that runs the automation defined by
automation.config.json, via either the Anthropic SDK (direct engine) or the
Claude Agent SDK (agent engine)."""

__version__ = "0.1.0"
